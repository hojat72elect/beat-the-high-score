
sourceControl {
    gitRepository("https://github.com/dwursteisen/libgdx-addons.git") {
        producesModule("com.github.dwursteisen.libgdx-addons:aseprite-plugin")
    }
}

include 'desktop', 'core'

if(hasProperty("ANDROID")) {
    include 'android'
}
